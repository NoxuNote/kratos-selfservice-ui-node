<div class="content">
    {{> header}}
    <div class="container">
        <h4>Vérifier votre adresse email.</h4>

        {{> messages messages=messages className="global"}}

        {{#if (eq state "passed_challenge")}}
            <p>Succès de la vérification!</p>
        {{/if}}

        <div id="ui">
          {{> ui submitLabel="Resend verification link" ui=ui}}
        </div>
    </div>
</div>
